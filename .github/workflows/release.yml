name: release
on:
  workflow_run:
    branches:
      - master
    types:
      - completed
    workflows:
      - conventional-commits

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: google-github-actions/release-please-action@v3.5.0
        with:
          release-type: simple
